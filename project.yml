name: CashBox
options:
  bundleIdPrefix: com.demo
  deploymentTarget:
    iOS: "16.0"

targets:
  CashBox:
    type: application
    platform: iOS
    sources:
      - CashBox
    assets: CashBox/CashBox/Assets.xcassets
    info:
      path: CashBox/Info.plist
      properties:
        UILaunchScreen: {}
